# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing these this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: repositorio uno
email: javier.iranzo.burriel@gmail.com
description: >
  Intento de hacer un inicio de documentación.
#description: > # this means to ignore newlines until "baseurl:"
#  Write an awesome description for your new site here. You can edit this
#  line in _config.yml. It will appear in your document head meta (for
#  Google search results) and in your feed.xml site description.
baseurl: "/repositoriouno/docs" # the subpath of your site, e.g. /blog
#url: "http://example.com" # the base hostname & protocol for your site
url: "https://pelos6.github.io" # the base hostname & protocol for your site
#twitter_username: jekyllrb
github_username:  pelos6

# Build settings
markdown: kramdown
theme: minima
# defino el alcance de los valores por defecto para evitar repetir el layout default
# atención a los tabuladores !!!!
defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"
# https://github.com/Painted-Fox/jekyll-site-menus
